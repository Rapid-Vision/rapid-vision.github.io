# ObjectLoader



## Methods
### `create_instance`
**Description**:

Create a single object instance from a loader.

**Signature**:

```python
def create_instance(self, name: str=None) -> 'Object'
```

**Arguments**:

- **`name`** : `str{:python}` — Instanced object name


**Returns**: `'Object'{:python}`

---
